# ===== FRONTEND DOCKERFILE =====
FROM node:18

WORKDIR /app

# Kopiujemy package.json i package-lock.json jako pierwszy krok
COPY package*.json ./

# Instalacja WSZYSTKICH zależności (jednorazowo)
RUN npm install --force

# Teraz dopiero kopiujemy resztę projektu
COPY . .

# Port aplikacji React
EXPOSE 3000

# Uruchomienie dev servera
CMD ["npm", "start", "--", "--host", "0.0.0.0"]
